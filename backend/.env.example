# Required — MySQL root password
LMS_DB_PASSWORD=change-me

# Required — initial admin password (used by AdminSeeder on first run)
LMS_ADMIN_PASSWORD=change-me

# Optional — Google OAuth 2.0 credentials (leave empty to disable)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
